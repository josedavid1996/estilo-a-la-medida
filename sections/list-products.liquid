    <div>
    holaaaaa
      {% for product in collections['joyeria'].products %}
          {{- product.title | link_to: product.url }}
        {% endfor %}
    </div>

{% schema %}
{
    "name": "Lista de productos",
    "class": "products-list",
    "settings": [
        {
        "type": "text",
        "id": "section_title_products",
        "label": "Titulo de la sección",
        "default": "Información importante"
        }
    ],
    "presets": [
        {
        "name": "Lista de productos",
        "category": "Custom content"
    }
  ]
}
{% endschema %}